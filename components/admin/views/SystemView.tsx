import { AlertTriangle, CheckCircle2, Database, Server, ShieldCheck, Wifi } from 'lucide-react';
import { systemAlerts } from '@/lib/admin/mock-data';

export default function SystemView() {
  const services = [['Vercel', 'Normal', Server], ['Supabase', 'Normal', Database], ['Gemini API', 'Normal', Wifi], ['Upstash Redis', 'Normal', ShieldCheck]] as const;
  return <div className="space-y-5"><section className="grid grid-cols-2 gap-3 lg:grid-cols-4">{services.map(([name,status,Icon]) => <div key={name} className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><div className="flex items-center justify-between"><Icon className="h-5 w-5 text-slate-500" /><CheckCircle2 className="h-4 w-4 text-emerald-600" /></div><p className="mt-4 text-xs font-extrabold text-slate-900">{name}</p><p className="mt-1 text-[10px] font-bold text-emerald-700">{status}</p></div>)}</section><section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex items-center gap-2 border-b border-slate-100 px-5 py-4"><AlertTriangle className="h-5 w-5 text-amber-500" /><div><h2 className="font-extrabold text-slate-950">Alert operasional</h2><p className="text-xs text-slate-500">Nanti bersumber dari logs, webhook, dan health checks.</p></div></div><div className="divide-y divide-slate-100">{systemAlerts.map((alert) => <div key={alert.id} className="px-5 py-4"><div className="flex flex-wrap items-start justify-between gap-2"><div><p className="text-xs font-extrabold text-slate-900">{alert.title}</p><p className="mt-1 text-[11px] text-slate-500">{alert.detail}</p></div><span className="text-[10px] text-slate-400">{alert.time}</span></div></div>)}</div></section></div>;
}
